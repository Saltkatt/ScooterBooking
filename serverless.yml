# Welcome to Serverless!
#
# This file is the main config file for your service.
# It's very minimal at this point and uses default values.
# You can always add more config options for more control.
# We've included some commented out config examples here.
# Just uncomment any of them to get that config option.
#
# For full config options, check the docs:
#    docs.serverless.com
#
# Happy Coding!

service: bookings-api

custom:
  bookingsTableName: 'bookings-${self:provider.stage}'

provider:
  name: aws
  runtime: java8
  stage: ${opt:stage, 'dev'}
  region: ${opt:region, 'us-east-1'}
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:Query
        - dynamodb:Scan
        - dynamodb:GetItem
        - dynamodb:PutItem
        - dynamodb:UpdateItem
        - dynamodb:DeleteItem
      Resource:
        - { "Fn::GetAtt": ["BookingsDynamoDBTable", "Arn" ] }
  environment:
    BOOKINGS_TABLE_NAME: ${self:custom.bookingsTableName}

package:
  artifact: 'target/${self:service}-${self:provider.stage}.jar'

functions:
  listUsers:
    handler: com.wirelessiths.ListUsersHandler
    events:
      - http:
          path: /bookings
          method: get
  getUser:
    handler: com.wirelessiths.GetUserHandler
    events:
      - http:
          path: /bookings/{id}
          method: get
  createUser:
    handler: com.wirelessiths.CreateUserHandler
    events:
      - http:
          path: /bookings
          method: post
  deleteUser:
    handler: com.wirelessiths.DeleteUserHandler
    events:
      - http:
          path: /bookings/{id}
          method: delete

resources:
  Resources:
    UsersDynamoDBTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:custom.bookingsTableName}
        AttributeDefinitions:
          - AttributeName: id
            AttributeType: S
          - AttributeName: scooterId
            AttributeType: S
          - AttributeName: userId
          AttributeType: S
        KeySchema:
          - AttributeName: bookingsId
            KeyType: HASH
          - AttributeName: startTime
            KeyType: RANGE
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1
